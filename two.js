var isTrue = true;

// if (isTrue) {
// 	console.log('yes');
// } else {
// 	console.log('no');
// }

var x = 2 + 5;

var yesOrNo = isTrue ? 'yes' : 'no';
console.log(yesOrNo);

// ternary should be used for very simple cases where the intent is unmistakable and obvious
